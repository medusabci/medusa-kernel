# Placeholder for the versioned on-disk format spec for serialized Recordings.
# Populated in K6 (truly versioned recording schema) with a Pydantic v2 model
# of the serialized Recording dict plus SCHEMA_VERSION.
